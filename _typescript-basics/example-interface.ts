export interface INumsExample {
    num3: number;
    num4: number;
}

// like declaring a type alis
// could also use "type variableNameHere" but this is being depricated
// because it is file specific??
